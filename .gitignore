# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# local env files
.env
.env*.local

# local file-store data (see lib/repositories/file-store.ts) — this is
# the JSON "database" used in local development without Supabase.
# Each developer/environment gets its own; do not commit it.
/.data

# misc
.DS_Store
*.pem
npm-debug.log*

# Cloudflare / OpenNext build output (regenerated on every deploy)
/.open-next/
/.wrangler/
.dev.vars
