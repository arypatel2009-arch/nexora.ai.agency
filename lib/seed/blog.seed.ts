import type { BlogPost } from "@/lib/types";

// Intentionally empty — no invented articles. The full blog architecture
// (categories, tags, author, SEO fields, reading time, related posts) is
// wired up and ready; posts get added through the Admin CMS.
export const blogPostsSeed: BlogPost[] = [];
