import type { TeamMember } from "@/lib/types";

// Intentionally empty — no invented team bios. Add real team members
// through the Admin CMS's "Manage Team" module.
export const teamSeed: TeamMember[] = [];
