import type { CareerListing } from "@/lib/types";

// Intentionally empty — no invented openings. Add real roles through
// the Admin CMS's "Careers" module.
export const careersSeed: CareerListing[] = [];
